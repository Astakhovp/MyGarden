
&НаСервере
Процедура НаборРастенийПриИзмененииНаСервере()
	Объект.Растения.Очистить();
	ТЗ = Объект.НаборРастений.Набор.Выгрузить();
	Объект.Растения.Загрузить(ТЗ);
	Объект.Урожай = Объект.НаборРастений.НоменклатураУрожая;
КонецПроцедуры

&НаКлиенте
Процедура НаборРастенийПриИзменении(Элемент)
	НаборРастенийПриИзмененииНаСервере();
КонецПроцедуры
